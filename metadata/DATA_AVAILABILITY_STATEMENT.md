# Data availability

The galaxy diagnostic uses the public SPARC radial-acceleration data and does not redistribute the third-party `RAR.mrt` table. No new observational data were generated. Synthetic fields, fixed random seeds, numerical tables, and deterministic stress-test outputs needed by the current manuscript are either regenerated by the repository code or stored as text/CSV/JSON regression artifacts under the relevant results directories.
